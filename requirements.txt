# Production dependencies (sync with pyproject.toml [project] dependencies)
pandas>=2.0
numpy>=1.24
xgboost>=2.0
scikit-learn>=1.3
scipy>=1.10
psycopg[binary]>=3.1   # src/store.py -- Postgres/TimescaleDB persistence (WP-1)

# Optional integrations (not needed for demos/tests):
# py-clob-client      # Polymarket CLOB
# redis               # hot best-bid/ask state

# For development tools, use: pip install -r requirements-dev.txt
# For testing only, use: pip install -e ".[test]"
# For full development, use: pip install -e ".[dev]"
